# 🛡️ دليل النسخ الاحتياطي والاسترجاع الشامل (Backup & Disaster Recovery Manual)

هذا الدليل يوضح بالتفصيل الشامل **كافة الطرق والآليات المتاحة** لإنشاء نسخ احتياطية واسترجاع نظام متجر **نسائم ليبيا** بالكامل في أي ظرف، سواءً أثناء التشغيل اليومي أو في حال انهيار السيرفر والانتقال إلى استضافة جديدة كلياً.

---

## 📑 الفهرس
1. [نظرة عامة ومحتويات النسخة الاحتياطية](#1-نظرة-عامة-ومحتويات-النسخة-الاحتياطية)
2. [الطريقة الأولى: عبر واجهة لوحة التحكم (Web Dashboard UI)](#2-الطريقة-الأولى-عبر-واجهة-لوحة-التحكم-web-dashboard-ui)
3. [الطريقة الثانية: سطر الأوامر واستعادة الكوارث (CLI Disaster Recovery)](#3-الطريقة-الثانية-سطر-الأوامر-واستعادة-الكوارث-cli-disaster-recovery)
4. [الطريقة الثالثة: الأتمتة اليومية المجدولة (Automated Cron Job)](#4-الطريقة-الثالثة-الأتمتة-اليومية-المجدولة-automated-cron-job)
5. [الطريقة الرابعة: النسخ اليدوي المباشر لقاعدة البيانات والوسائط (Manual Raw Dump)](#5-الطريقة-الرابعة-النسخ-اليدوي-المباشر-لقاعدة-البيانات-والوسائط-manual-raw-dump)
6. [مقارنة سريعة: متى تستخدم كل طريقة؟](#6-مقارنة-سريعة-متى-تستخدم-كل-طريقة)
7. [أفضل الممارسات الأمنية لحفظ النسخ](#7-أفضل-الممارسات-الأمنية-لحفظ-النسخ)

---

## 1. نظرة عامة ومحتويات النسخة الاحتياطية

يقوم محرك النسخ الاحتياطي في النظام بتوليد حزمة مضغوطة واحدة بصيغة **`.zip`** (مثل `nasaeem_backup_20260829_120000.zip`) تحتوي ذاتياً على كل ما يلزم لإعادة بناء النظام من الصفر:

| المكون داخل الـ ZIP | الوصف والمحتوى |
|---|---|
| 📄 `database.json` | تفريغ تسلسلي كامل (JSON Fixtures) لجميع الجداول: المنتجات، النوتات العطرية، الحسابات والعملاء، الطلبات، الفواتير، المدن، التخصيصات، ودليل الأستاذ. |
| 🖼️ `media/` | مجلد الوسائط بالكامل متضمناً صور المنتجات، الشعارات، وبطاقات الهوية والوسائط المرفوعة بدقتها الأصلية. |
| 📋 `manifest.json` | ملف وصف تحليلي يتضمن البصمة الزمنية، نسخة النظام، وإحصائيات أعداد السجلات وحجم البيانات للتحقق من سلامة الأرشيف قبل فكه. |

- **المسار الافتراضي للنسخ على السيرفر**:  
  `store-app/backend/backups/`

---

## 2. الطريقة الأولى: عبر واجهة لوحة التحكم (Web Dashboard UI)

هذه الطريقة الأسهل للاستخدام اليومي المباشر عبر المتصفح دون الحاجة لكتابة أوامر برمجية.

### 📍 الوصول للقسم:
سجل دخول كمدير للنظام (`Admin / Owner`) ثم انتقل إلى:
```
https://your-domain.com/admin/backup
```
(أو من القائمة الجانبية: **«التخصيص وإدارة النظام»** ⬅️ **«النسخ الاحتياطي للنظام»**).

---

### أ) إنشاء نسخة احتياطية جديدة وتنزيلها:
1. اضغط على الزر الأزرق العلوي: **«إنشاء نسخة احتياطية شاملة الآن»**.
2. سيقوم النظام بجمع البيانات وضغط الصور وحفظ الملف في ثوانٍ.
3. ستظهر النسخة فوراً في جدول **«سجل النسخ الاحتياطية المحفوظة»** موضحاً حجمها وتاريخها وعدد السجلات.
4. اضغط على زر **«تنزيل»** لحفظ ملف الـ `.zip` مباشرة إلى حاسوبك الشخصي.

---

### ب) استرجاع النظام من نسخة محفوظة مسبقاً على السيرفر:
1. في جدول النسخ الاحتياطية، حدد النسخة التي تريد العودة إليها.
2. اضغط على زر **«استرجاع»** الرمادي المقابل لها.
3. ستظهر لك نافذة تأكيد أمنية ⬅️ اضغط **«نعم، ابدأ الاسترجاع الآن»**.
4. سيتم تحديث قاعدة البيانات والصور فوراً، وستظهر رسالة نجاح خضراء بعدد السجلات المسترجعة.

---

### ج) استرجاع النظام برفع ملف خارجي (.zip):
1. في نفس الصفحة، توجه إلى قسم **«استرجاع من ملف خارجي (.zip)»**.
2. اضغط على **«اختيار ملف النسخة الاحتياطية»** واختر ملف الـ `.zip` المخزن على حاسوبك.
3. سيظهر اسم وحجم الملف المختار وزر **«بدء الاسترجاع الفوري الآن»**.
4. اضغط عليه لتأكيد الرفع وفك البيانات وإعادة النظام إلى حالته المحفوظة في ذلك الملف.

---

## 3. الطريقة الثانية: سطر الأوامر واستعادة الكوارث (CLI Disaster Recovery)

**هذه هي الطريقة الذهبية والأهم عند الكوارث** (مثلاً: تم حذف السيرفر بالكامل، تعطل الاستضافة، أو الرغبة في نقل المتجر إلى خادم جديد تماماً).

### 🚨 سيناريو العمل على سيرفر جديد فارغ من الصفر:

#### الخطوة 1: تجهيز السيرفر وسحب الكود
```bash
# الانتقال لمجلد المشاريع واستنساخ المشروع
cd /var/www/html  # أو مسار الاستضافة في cPanel / home
git clone https://github.com/zakariaali99/nasaeem-libya.git
cd nasaeem-libya/store-app/backend
```

#### الخطوة 2: إنشاء البيئة الافتراضية وتثبيت المكتبات
```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

#### الخطوة 3: تهيئة قاعدة البيانات الجديدة
```bash
python manage.py migrate
```
*(الآن قاعدة البيانات جاهزة وهيكليتها فارغة، ولا توجد أي منتجات أو حسابات)*.

#### الخطوة 4: رفع ملف النسخة الاحتياطية للسيرفر
ارفع ملف الـ ZIP (مثلاً `nasaeem_backup_20260829.zip`) إلى السيرفر بأي وسيلة:
- عبر **cPanel File Manager** (مدير الملفات).
- أو عبر **FTP / FileZilla**.
- أو عبر أمر **SCP** في سطر الأوامر:
  ```bash
  scp /local/path/nasaeem_backup.zip user@server_ip:/var/www/html/nasaeem-libya/store-app/backend/backups/
  ```

#### الخطوة 5: استرجاع النظام بالكامل بأمر واحد:
من مجلد `store-app/backend` شغل:
```bash
python manage.py restore_system_backup /path/to/nasaeem_backup.zip
```
*(أو تخطي طلب التأكيد للأتمتة)*:
```bash
python manage.py restore_system_backup /path/to/nasaeem_backup.zip --no-input
```

#### ⚡ ما يحدث خلف الكواليس تلقائياً:
1. يتحقق الخادم من سلامة ملف الأرشيف وبيانات الـ Manifest.
2. يستخرج مجلد الصور والوسائط بالكامل إلى مساره الرسمي المعتمد `media/`.
3. يحقن جميع السجلات (المنتجات، الحسابات، الطلبات، الكتالوج، الحزم) داخل معاملة ذرية آمنة (`Atomic Transaction`).
4. **النظام يعمل فوراً وبنفس حسابات المديرين والعملاء السابقة دون الحاجة حتى لإنشاء حساب مدير مؤقت.**

---

## 4. الطريقة الثالثة: الأتمتة اليومية المجدولة (Automated Cron Job)

لحماية متجرك من أي طارئ دون تدخل يدوي، تم توفير أمر سطر الأوامر لتوليد النسخ:
```bash
python manage.py create_system_backup
```

### ⏰ كيفية ضبط النسخ الاحتياطي التلقائي:

#### أ) في لوحة تحكم الاستضافة (cPanel Cron Jobs):
1. افتح لوحة تحكم cPanel وانتقل إلى **Cron Jobs** (المهام المجدولة).
2. اضغط **Add New Cron Job**.
3. اضبط التوقيت (مثلاً يومياً الساعة 2:00 صباحاً):
   - **Minute**: `0`
   - **Hour**: `2`
   - **Day**: `*`
   - **Month**: `*`
   - **Weekday**: `*`
4. في خانة **Command** ضع:
   ```bash
   cd /home/username/nasaeemlibya/store-app/backend && /home/username/nasaeemlibya/store-app/backend/.venv/bin/python manage.py create_system_backup >> /home/username/backup_cron.log 2>&1
   ```

#### ب) في سيرفرات لينكس العادية (Linux VPS / Crontab):
افتح محرر المهام المجدولة:
```bash
crontab -e
```
أضف السطر التالي:
```bash
0 2 * * * cd /var/www/html/nasaeem-libya/store-app/backend && .venv/bin/python manage.py create_system_backup >> /var/log/nasaeem_backup.log 2>&1
```

---

## 5. الطريقة الرابعة: النسخ اليدوي المباشر لقاعدة البيانات والوسائط (Manual Raw Dump)

إذا أردت في أي وقت تصدير واستيراد البيانات يدوياً عبر أدوات دجانغو الافتراضية:

### أ) تصدير البيانات يدوياً:
```bash
cd store-app/backend
source .venv/bin/activate

# تصدير قاعدة البيانات لملف JSON مع استثناء جلسات وصلاحيات النظام الافتراضية
python manage.py dumpdata --natural-foreign --natural-primary -e contenttypes -e auth.Permission --indent 2 > full_database_dump.json

# ضغط مجلد الصور
tar -czvf media_assets.tar.gz media/
```

### ب) استيراد البيانات يدوياً:
```bash
# فك مجلد الصور
tar -xzvf media_assets.tar.gz

# حقن البيانات
python manage.py loaddata full_database_dump.json
```

---

## 6. مقارنة سريعة: متى تستخدم كل طريقة؟

| السيناريو | الطريقة الموصى بها | السبب |
|---|---|---|
| **نسخ احتياطي روتيني أثناء العمل اليومي** | واجهة الويب (`/admin/backup`) | سريعة بنقرة زر واحدة وتنزيل مباشر لجهازك. |
| **تثبيت المتجر على استضافة جديدة بعد حذف القديم** | سطر الأوامر (`restore_system_backup`) | لا تشترط وجود متصفح أو حساب مسبق وتتجاوز أي قيود في أحجام رفع السيرفر. |
| **الحماية الدورية من نسيان أخذ النسخ** | الأتمتة المجدولة (`Cron Job`) | تعمل ذاتياً كل ليلة وتضمن عدم ضياع أي طلبات جديدة. |
| **نقل صور ضخمة جداً (عشرات الجيجابايت)** | ضغط المجلد الخارجي (`tar / rsync`) | لنقل الملفات الخام مباشرة بين السيرفرات بسرعات شبكية عالية. |

---

## 7. أفضل الممارسات الأمنية لحفظ النسخ

1. **قاعدة 3-2-1 في النسخ الاحتياطي**:
   - احتفظ بـ **3** نسخ من بياناتك.
   - على نوعين مختلفين من الوسائط (**2**): على السيرفر، وعلى جهازك المكتبي أو قرص صلب خارجي.
   - نسخة واحدة خارج موقع السيرفر بالكامل (**1**): مثلاً على Google Drive أو Dropbox أو S3.
2. **تنزيل أسبوعي دوري**:
   - ادخل كل أسبوع إلى `/admin/backup` واضغط **«تنزيل»** لأحدث نسخة واحتفظ بها على حاسوبك.
3. **تنظيف السيرفر**:
   - لا تترك مئات النسخ القديمة جداً على السيرفر لتفادي استهلاك مساحة القرص؛ يمكنك حذف النسخ القديمة عبر زر الحذف في الجدول بعد تنزيلها.
